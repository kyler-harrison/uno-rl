class Opponent(Player):

    def __init__():
        super().__init__()

    def decision(self, top_card):
        """
        implements opponent strategy defined in what.txt, calls play_card()
        top_card: the top card on the play deck (defined in game)
        """

        # TODO define simple strategy from what.txt, i.e. action opponent should take
        # should take in current top card from deck only bc this the strat player?
        pass
